#!/bin/sh
bold="$(tput bold)"
normal="$(tput sgr0)"
if hash mamba; then
    echo mamba
elif hash micromamba; then
    echo micromamba
else
    echo echo "❌  ${bold}Could not find micromamba or mamba, please install and try again.${normal}" >&2
    echo echo "${bold}FAILED:${normal} mamba "
fi
