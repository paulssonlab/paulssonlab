channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - bowtie2==2.4.2
  - samtools==1.11
  - tbb==2020.2 # needed to avoid "dyld: Library not loaded: @rpath/libtbb.dylib" on macOS (210303)
